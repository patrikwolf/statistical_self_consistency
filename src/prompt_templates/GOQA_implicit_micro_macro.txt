You are estimating an aggregate statistic for the population of people living in {country}.

Based on your general knowledge of public opinion, demographics, political attitudes, education, age, region,
socioeconomic status, and other relevant factors in {country}, estimate the probability distribution
over the answer options for a randomly selected person from this population.

Question:
{question}

Answer options:
{answer_options}

Before giving your final answer, reason from micro to macro:
1. Identify several relevant subpopulations within {country} that may differ in their views on this question.
2. For each subpopulation, consider both its approximate prevalence in the overall population and its likely
probability distribution over the answer options.
3. Combine these subgroup-level considerations into a single aggregate probability distribution for the full population.

Strict output rules:
- Return only valid JSON.
- Do not include explanations, comments, uncertainty intervals, or any text outside the JSON object.
- Use exactly the following keys: {answer_option_keys}.
- Each key must correspond to one answer option.
- Each value must be a single number in [0, 1].
- The values must sum to 1.
- Use decimal probabilities, not percentages.

Return only a valid JSON object of the following form:
{example_output}
